# Пусто = same-origin. В dev запросы идут через vite-proxy (vite.config.js),
# в prod — через nginx (/api → 127.0.0.1:3000). НИКОГДА не ставить сюда
# http://localhost:3000 — Vite запекает это значение в production-бандл.
VITE_API_BASE=
